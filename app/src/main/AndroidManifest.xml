<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ranad_000.fitness_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SplashScreen" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.SecondActivity" />
        <activity android:name=".Activities.nutritionActivity" />
        <activity android:name=".Activities.WorkoutActivity" />
        <activity android:name=".chestWorkoutActivity"></activity>
        <activity android:name=".Activities.LegWorkoutActivity" />
        <activity android:name=".Activities.ShoulderWorkoutActivity"></activity>
    </application>

</manifest>